"""Tests backend de BreastAI.

Ce fichier fait de `tests` un paquet : pytest importe alors conftest comme
`tests.conftest`, ce qui permet aux modules de test de réutiliser ses aides
(`login`, `auth_headers`) sans en créer une seconde copie en mémoire.
"""
