-r requirements.txt

pytest==8.3.4
pytest-asyncio==0.25.2
pytest-cov==6.0.0
ruff==0.8.6

# Relecture des PDF produits : les tests vérifient le contenu réel du document,
# pas seulement qu'un fichier a été écrit.
pypdf==5.1.0
