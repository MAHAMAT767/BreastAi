# ---------- API ----------
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.5
pydantic-settings==2.7.1
python-multipart==0.0.20

# ---------- Base de données (Phase 2) ----------
sqlalchemy==2.0.36
alembic==1.14.0
psycopg[binary]==3.2.3

# ---------- Sécurité (Phase 2) ----------
bcrypt==4.2.1
pyjwt==2.10.1
email-validator==2.2.0
slowapi==0.1.9

# ---------- Imagerie (Phase 3) ----------
numpy==2.2.1
# Variante « headless » : la variante standard tire des bibliothèques GUI
# (GTK, X11) inutiles et lourdes dans une image Docker serveur.
opencv-python-headless==4.11.0.86
pillow==11.1.0
pydicom==3.0.1

# Les dépendances IA (torch, torchvision, albumentations, grad-cam, onnxruntime)
# et PDF (reportlab) seront ajoutées aux phases 4 et 5.
