# ---------- API ----------
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.5
pydantic-settings==2.7.1
python-multipart==0.0.20

# ---------- Base de données (Phase 2) ----------
sqlalchemy==2.0.36
alembic==1.14.0
psycopg[binary]==3.2.3

# ---------- Sécurité (Phase 2) ----------
bcrypt==4.2.1
pyjwt==2.10.1
email-validator==2.2.0
slowapi==0.1.9

# ---------- Imagerie (Phase 3) ----------
numpy==2.2.1
# Variante « headless » : la variante standard tire des bibliothèques GUI
# (GTK, X11) inutiles et lourdes dans une image Docker serveur.
opencv-python-headless==4.11.0.86
pillow==11.1.0
pydicom==3.0.1
# pydicom ne décode pas seul les transfer syntax compressées. Sans ces greffons,
# un DICOM en JPEG Lossless ou JPEG 2000 — courant sur les mammographes — est
# rejeté alors que le fichier est parfaitement valide.
pylibjpeg==2.1.0
pylibjpeg-libjpeg==2.3.0
pylibjpeg-openjpeg==2.4.0

# ---------- Modèle (Phase 4) ----------
# Sur Windows et macOS, les roues PyPI sont déjà des builds CPU. Sur Linux, la
# roue PyPI embarque CUDA et pèse plus de 2 Go : l'image Docker installe donc
# explicitement les roues CPU depuis download.pytorch.org avant ce fichier
# (voir docker/Dockerfile.backend).
torch==2.13.0
torchvision==0.28.0

# Grad-CAM est implémenté dans app/ai/explainability/ plutôt que via la
# bibliothèque grad-cam : une trentaine de lignes de hooks, contre une
# dépendance de plus à suivre.
#
# Les dépendances PDF (reportlab) seront ajoutées en Phase 5.
